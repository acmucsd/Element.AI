# Replay Generator

## Usage

```
python replay.py replay.json -o output_vid
```

The cli args are as follows:
- parg: replay file (be sure to include `.json` here)
- `-o`&emsp;The desired name of the replay video. Note: the `.mp4` extension will be added by default.